{
  "name": "tree-sitter-folio",
  "version": "0.0.1",
  "description": "folio grammar for tree-sitter (dialogue/scripting layer over lish)",
  "license": "MIT",
  "files": [
    "grammar.js",
    "queries/*.scm",
    "src/**",
    "common/**",
    "scripts/**"
  ],
  "scripts": {
    "sync": "node scripts/sync.js",
    "pregenerate": "node scripts/sync.js",
    "postinstall": "node scripts/sync.js --soft",
    "generate": "tree-sitter generate",
    "test": "tree-sitter test && node test/scanner-corpus.test.js"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.24.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["tree-sitter-cli"]
  }
}
